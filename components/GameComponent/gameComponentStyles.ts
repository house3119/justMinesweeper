import { StyleSheet } from "react-native";


const gameComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center'
  }
})

export default gameComponentStyles;